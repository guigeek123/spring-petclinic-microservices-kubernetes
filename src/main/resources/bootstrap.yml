server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/guigeek123/spring-petclinic-microservices-kubernetes-config
---
spring:
  profiles: local
  cloud:
    config:
      server:
        git:
          uri: file:///${GIT_REPO}
